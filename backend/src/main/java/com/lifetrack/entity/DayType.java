package com.lifetrack.entity;

/** User-selected context used to compare wellbeing across different kinds of days. */
public enum DayType {
    STUDY_WORK,
    DAY_OFF,
    TRAVEL,
    SICK,
    UNUSUAL
}
